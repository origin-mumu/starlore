name: starlore_app
description: "Starlore — 星语治愈，星座运势与 AI 对话"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  http: ^1.2.0
  flutter_markdown: ^0.7.4
  intl: ^0.19.0
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
